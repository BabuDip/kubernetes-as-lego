apiVersion: v1
kind: Service
metadata:
  name: django
  labels:
    app: django
    component: web
spec:
  selector:
    app: django
    component: web
  ports:
    - name: http
      port: 8000
      targetPort: http
