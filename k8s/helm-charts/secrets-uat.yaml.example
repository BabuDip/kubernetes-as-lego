# Copy this file to secrets-uat.yaml (git-ignored) and fill in real values
# before installing with -f values-uat.yaml -f secrets-uat.yaml. Never commit
# secrets-uat.yaml.
secrets:
  djangoSecretKey: changeme
  postgresUser: qless_cafe
  postgresPassword: changeme
